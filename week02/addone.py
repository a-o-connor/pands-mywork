#want this program to take an input number from user and add one to it 
#first line will be asking for the input number, preface with int I want to only accept integers 
number = int(input("Pick a number!"))
newnumber = number + 1
print(f"{number} plus one is {newnumber}") 

